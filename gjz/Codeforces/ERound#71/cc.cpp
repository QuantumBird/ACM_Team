#include <iostream>
#include <algorithm>
#include <string>
#include <cstring>
using namespace std;
typedef long long ll;
int main(){
    int T;
    cin>>T;
    while(T --){
        int n, a, b;
        string s;
        cin>>n>>a>>b>>s;
        int sp = 0;
        ll ans = 0;
        int sp = 0;
        while(sp < n){
            int tmp = sp;
            int cnt = 0;
            while(s[tmp] == s[sp]){
                sp ++;
                cnt ++;
            }
            if(flg == false){
                ans += 
            }
        }
        for(int i = 0; i < np; i ++)
            cout<<note[i]<<endl;
    }
}