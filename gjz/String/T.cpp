#include <iostream>
#include <string>
#include <cstring>
using namespace std;
const int maxn = 1e6 + 1;
int tire[maxn][10];
int nptr;
void init() {
    memset(tire, 0, sizeof(tire));
    nptr = 1;
}
bool insert(string & s) {

int main() {
    return 0;
}
