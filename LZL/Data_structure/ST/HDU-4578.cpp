#include<iostream>
#include<algorithm>
#include<string>
#include<map>
#include<set>
#include<map>
#include<set>
#include<cmath>
#include<queue>
#include<stack>
#include<vector>
#include<cstdlib>
#include<utility> 
#include<cstring>
#include<cstdio>
using namespace std;

#define lson (q<<1)
#define rson (q<<1|1)
#define mid ((l+r)>>1)
const int maxn = 100000+10;

int segt[maxn<<2];
int book[maxn];
int log_mul[maxn<<2];
int log_add[maxn<<2];
int log_equ[maxn<<2];
int m,n,flag,lhs,rhs,w;

void update_add(int q,int l,int r,int lhs,int rhs){
    
}

int main(){
    std::ios::sync_with_stdio(false);
    cin >> n >> m;
    for(int i=0;i<m;++i){
        cin >> flag >> lhs >> rhs >> w;

    }
}