#include<bits/stdc++.h>
using namespace std;

int m,n,q,tmp;

int main(){
    std::ios::sync_with_stdio(false);
    cin >> q;
    while(q--){
        cin >> m >> n;
        int flag = m;
        for(int i=0;i<n;++i){
            cin >> tmp;
        }
    }
}